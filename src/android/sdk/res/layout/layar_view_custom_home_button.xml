<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/custom_home_button"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:layout_gravity="left"
    android:clickable="true" >

    <ImageView
        android:id="@+id/custom_home_icon"
        android:layout_width="60dp"
        android:layout_height="match_parent"
        android:layout_centerVertical="true"
        android:background="@drawable/custom_home_button"
        android:scaleType="center" />

    <TextView
        android:id="@+id/custom_home_label"
        style="@style/CustomHomeLabel"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_centerVertical="true"
        android:layout_toRightOf="@id/custom_home_icon"
        android:background="@drawable/custom_home_button"
        android:gravity="center"
        android:paddingLeft="0dp"
        android:paddingRight="20dp"
        android:text="Geo Layers"
        android:visibility="gone" />

</RelativeLayout>